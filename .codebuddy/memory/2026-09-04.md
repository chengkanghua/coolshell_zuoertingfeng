# 2026-09-04

## .gitignore 创建（coolshell_zuoertingfeng）
- 在仓库根目录新建 `.gitignore`,忽略内容：`.venv/`、`__pycache__/`、`*.py[cod]`、`.idea/`、`.vscode/`、系统文件(`.DS_Store`/`Thumbs.db`/`desktop.ini`)、mkdocs 构建产物 `site/`。
- `my-project/` 未忽略,仍显示为 untracked（mkdocs 站点,docs/ + mkdocs.yml,未决定是否入库）。
- 仓库内容：收集陈皓《左耳听风》课程/书籍资料;根目录另有 `books/`、`左耳听风-*/` 已跟踪。

## mkdocs.yml 全量导航更新（my-project）
- `my-project/` 为 mkdocs 站点(theme: material),`.venv` 中已装 mkdocs 1.6.1(python 3.12)。
- 将 docs/ 下 125 个 md 全部写入 nav（移除 README.md 后共 124 个导航叶子;`docs/index.md` 为主页）。导航标题取各文件首个 `#` H1,顺序按文件名自然排序;`docs/refer/` 归入「参考资料」分组。
- 关键坑:mkdocs nav 路径相对 docs_dir,不能带 `docs/` 前缀;且 index.md 与 README.md 并存时 README.md 会被 mkdocs 忽略,无法导航。
- 源文档(如 000-1目录.md)内部旧链接大量失效(229 个 warning),非配置问题,故用普通 build 验证而非 --strict;普通 build 已零报错。
- 目录内容为极客时间《左耳听风》课程文章(000-开篇词 ~ 118-加餐,按 NNN-编号)。
